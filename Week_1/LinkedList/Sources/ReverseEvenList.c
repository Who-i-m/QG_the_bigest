/**
 *  @name        : LNode* ReverseEvenList(LinkedList *L)
 *	@description : reverse the nodes which value is an even number in the linked list, input: 1 -> 2 -> 3 -> 4  output: 2 -> 1 -> 4 -> 3（将链表中数值为偶数的节点倒过来，输入。1 -> 2 -> 3 -> 4 输出。2 -> 1 -> 4 -> 3） 
 *	@param		 : L(the head node)
 *	@return		 : LNode(the new head node)
 *  @notice      : choose to finish
 */
 
#include"linkedlist.h"
#include<stdlib.h>
#include<stdio.h>

LNode* ReverseEvenList(LinkedList *L){
	
}
